# 2층 스키마 설계안 (0단계 확정판)

> 2026-09-08 확정. 도도 검토·답변 반영 완료.

## A. 현황 조사 결과

### A-1. 인증: 이미 완전 구현됨

| 항목 | 상태 | 위치 |
|------|------|------|
| users 테이블 | 운영 중 (1명: id=1, google, TestUser) | `init_db.sql` |
| JWT | HS256, 7일 만료, Bearer 토큰 | `backend/app/auth/jwt.py` |
| OAuth 3사 | Google/Kakao/Naver 콜백 구현 완료 | `backend/app/auth/oauth.py` |
| get_current_user() | 모든 API 엔드포인트에 적용 | `backend/app/auth/deps.py` |
| 프론트 연동 | AuthContext + localStorage + Axios 인터셉터 | `frontend/src/contexts/AuthContext.jsx` |
| 로그인 페이지 | MVP (수동 토큰 입력, OAuth UI 미구현) | `frontend/src/pages/LoginPage.jsx` |

1단계 할 일: 프론트 OAuth UI(소셜 로그인 버튼 → 리다이렉트 → 콜백)만 구현.

### A-2. 데이터 구조 & 소유 관계

```
User (users.id)
 └─ Project (projects.user_id FK) ← 최상위 소유 단위
     ├─ Episode (episodes.project_id)
     │   ├─ Cut (cuts.episode_id)
     │   ├─ Location (locations.episode_id)
     │   └─ Style (styles.episode_id)
     ├─ Series (series.project_id)
     ├─ Character (characters.project_id + episode_id)
     └─ ProjectMemory (project_memory.project_id)
```

- `projects.user_id`가 유일한 소유권 FK. 하위 엔티티는 project를 통해 간접 소유.
- 추가 FK 불필요.

### A-3. 이미지 생성 호출 지점 (패킷 차감 훅 대상)

모든 이미지 생성은 `GeminiImageAdapter.generate_image()` 경유.

| # | 파일 | 함수 | 생성 대상 | 패킷 |
|---|------|------|-----------|------|
| 1 | `images/service.py` | `generate_cut_image()` | 컷 이미지 | **1** |
| 2 | `characters/service.py` | `generate_character_sheets()` | 캐릭터 시트 (정면+표정) | **2** |
| 3 | `locations/service.py` | `generate_location_images()` | 장소 레퍼런스 | **1** |
| 4 | `locations/service.py` | `convert_photo_to_illustration()` | 사진→일러스트 | **1** |
| 5 | `characters/service.py` | `extract_appearance_from_photos()` | 외형 추출 (텍스트 전용) | **0** (무료) |

### A-4. 프론트 라우팅

| 경로 | 페이지 | 인증 |
|------|--------|------|
| `/login` | LoginPage | 공개 |
| `/bubble-test` | BubbleTestPage | 공개 |
| `/` | DashboardPage | 보호 |
| `/projects/:projectId` | ProjectPage | 보호 |
| `/projects/:projectId/episodes/:episodeId/workflow` | WorkflowPage | 보호 |
| `/projects/:projectId/series/:seriesId` | SeriesPage | 보호 |

React Router v7, `basename="/WEBTOON"`. 새 페이지: `pages/` 에 컴포넌트 → `App.jsx` Route 추가.

### A-5. 배포 구조

- HTTPS: Apache 2.4 (Bitnami), 완전 HTTPS
- 리버스 프록시: `/WEBTOON/` → `localhost:8000`
- 토큰: localStorage Bearer (MVP OK)
- SECRET_KEY: 1단계에서 랜덤 생성해 운영 .env 설정
- OAuth redirect URI: config.py 기본값이 localhost → 운영 .env에서 ssagda.com으로 오버라이드

---

## B. DB 스키마 설계 (확정)

### 설계 방침

기존 과금 테이블(`subscriptions`, `credit_balances`, `credit_transactions`)은 **삭제하지 않고 유지** (이력 보존, 롤백 안전). 서비스 레이어만 새 패킷 테이블로 전환.

### B-1. `packet_balances`

```sql
CREATE TABLE IF NOT EXISTS packet_balances (
  user_id       BIGINT PRIMARY KEY,
  balance       INT NOT NULL DEFAULT 0,
  updated_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id)
);
```
- 가입 시 balance=0 (무료 패킷 없음)
- 증가: 구매(purchase) 또는 관리자 지급(admin_grant)

### B-2. `packet_transactions`

```sql
CREATE TABLE IF NOT EXISTS packet_transactions (
  id            BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id       BIGINT NOT NULL,
  delta         INT NOT NULL,
  reason        ENUM('purchase','generation','refund','admin_grant') NOT NULL,
  ref_log_id    BIGINT,
  balance_after INT NOT NULL,
  created_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id),
  INDEX idx_user_time (user_id, created_at)
);
```
- `generation`: 이미지 생성 시 delta=-1
- `refund`: 생성 실패 시 delta=+1
- `purchase`: 토스페이먼츠 결제 (4단계)
- `admin_grant`: 수동 지급 (도도용)

### B-3. `notices`

```sql
CREATE TABLE IF NOT EXISTS notices (
  id            BIGINT PRIMARY KEY AUTO_INCREMENT,
  title         VARCHAR(200) NOT NULL,
  body          TEXT,
  notice_type   ENUM('info','warning','maintenance','update') NOT NULL DEFAULT 'info',
  is_active     TINYINT(1) NOT NULL DEFAULT 1,
  starts_at     DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  ends_at       DATETIME,
  created_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

### B-4. `generation_logs` 변경

```sql
ALTER TABLE generation_logs ADD COLUMN packets_charged INT NOT NULL DEFAULT 0;
```
기존 `credits_charged` 컬럼은 유지 (이력 보존).

---

## C. 마이그레이션 계획

### C-1. step18.sql / step18_down.sql

**step18.sql:**
```sql
-- Step 18: 패킷 과금 시스템 + 공지 테이블

CREATE TABLE IF NOT EXISTS packet_balances (
  user_id       BIGINT PRIMARY KEY,
  balance       INT NOT NULL DEFAULT 0,
  updated_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE IF NOT EXISTS packet_transactions (
  id            BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id       BIGINT NOT NULL,
  delta         INT NOT NULL,
  reason        ENUM('purchase','generation','refund','admin_grant') NOT NULL,
  ref_log_id    BIGINT,
  balance_after INT NOT NULL,
  created_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id),
  INDEX idx_user_time (user_id, created_at)
);

CREATE TABLE IF NOT EXISTS notices (
  id            BIGINT PRIMARY KEY AUTO_INCREMENT,
  title         VARCHAR(200) NOT NULL,
  body          TEXT,
  notice_type   ENUM('info','warning','maintenance','update') NOT NULL DEFAULT 'info',
  is_active     TINYINT(1) NOT NULL DEFAULT 1,
  starts_at     DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  ends_at       DATETIME,
  created_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

ALTER TABLE generation_logs ADD COLUMN packets_charged INT NOT NULL DEFAULT 0;

-- 기존 사용자 패킷 잔량 초기화 (전원 0 시작)
INSERT IGNORE INTO packet_balances (user_id, balance) SELECT id, 0 FROM users;

INSERT INTO schema_migrations (version) VALUES ('step18');
```

**step18_down.sql:**
```sql
ALTER TABLE generation_logs DROP COLUMN packets_charged;
DROP TABLE IF EXISTS packet_transactions;
DROP TABLE IF EXISTS packet_balances;
DROP TABLE IF EXISTS notices;
DELETE FROM schema_migrations WHERE version = 'step18';
```

### C-2. 기존 데이터 귀속

1. 도도가 소셜 로그인으로 최초 가입 → users에 새 레코드 (예: id=2)
2. 귀속: `UPDATE projects SET user_id = 2 WHERE user_id = 1;`
3. 하위 엔티티는 project를 통해 간접 소유 → 변경 불필요
4. TestUser(id=1)는 비활성 유지 (삭제 안 함)
5. 크레딧→패킷 전환 없음. 전원 0 시작, 도도 계정은 admin_grant로 지급

### C-3. 리허설 절차

1. `project_t_test` DB 생성 (운영 스키마 복제, `mysqldump --no-data`)
2. step18.sql 적용 → 테이블 확인
3. step18_down.sql 적용 → 롤백 확인
4. 재적용 → 안정성 확인
5. 도도 승인 대기
6. 운영 적용 직전: `mysqldump --single-transaction project_t > backup_before_step18.sql`
7. 운영 DB에 step18.sql 1회 적용

---

## 확정된 패킷 차감 규칙

| 작업 | 패킷 | 비고 |
|------|------|------|
| 컷 이미지 1개 | 1 | |
| 캐릭터 시트 1명 | 2 | 정면 1 + 표정 1 (버튼에 "2패킷 사용" 표시는 3단계) |
| 장소 레퍼런스 1개 | 1 | |
| 사진→일러스트 1건 | 1 | 비전 분석은 텍스트=무료 |
| 외형 추출 (텍스트) | 0 | LLM 작업=무료 |
| 생성 실패 | 0 | 미차감 또는 자동 환불 |
