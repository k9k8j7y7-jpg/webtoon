# 핸드오프 2026-09-26 — 외장하드 복구 · 환경 재구성 · 가격 정책

## 경위

외장하드(F:) 고장으로 기존 작업 디렉터리 손실.
GitHub에서 새로 clone하고, 복구본(F:\samsung)에서 필요 파일만 이전.

## 이번 세션에서 수행한 작업

### 1. 저장소 복원
- `C:\vibecoding\WEBTOON`에 GitHub clone (최신 `b57a3cd` 확인)
- `.env` 2건 복구본에서 복사: `backend/.env` (23키), `frontend/.env.local` (1키)

### 2. 의존성 설치
- **프론트엔드:** npm install + `npm run build` 성공
- **백엔드:** Python 3.14.6에서 pydantic-core 빌드 실패 → Python 3.12.10 설치 → venv 재생성 → pip install 성공

### 3. 정적 에셋 git 추적 (`25bb178`)
- `frontend/public/bubbles/` (5 PNG + meta JSON, 508KB) — 복구본에서 복사, git add
- `frontend/public/effects/` (10 PNG, 1.3MB) — 복구본에서 복사, git add
- 이전에 git 미추적이어서 clone 시 빌드 실패 원인이었음. 이제 추적됨

### 4. SSH 접속 복원
- 키: `C:\Users\user\.ssh\DONGHAESSHKEy.pem` (Downloads에서 복사)
- `bitnami@52.79.94.122` 접속 성공 (uptime 173일)
- CLAUDE.md 키 경로 2곳 갱신

### 5. 패킷 가격 정책 재설정 (`cc13d5b`)
- 상품 가격: 30개 6,900원 / 100개 19,900원 / 300개 54,900원
- 순마진 계단: 45% / 40% / 35% (원가 93원, gemini-3.1-flash-image 기준)
- CLAUDE.md에 가격 정책 원칙 + 작업 환경 원칙 추가

## 미해결

- **가격 반영:** /admin에서 실제 상품 가격 변경 필요 (코드 변경 없음, DB 값만)
- **16:9 실생성 검증:** ep32 슛돌이 단편 대기 중
- **Python 3.14:** 시스템에 남아있음. 백엔드 venv는 3.12 사용 중

## 커밋 이력

| 해시 | 내용 |
|---|---|
| `25bb178` | 정적 에셋 누락분 추가(특수 말풍선·효과 PNG) |
| `cc13d5b` | 패킷 가격 정책 재설정 + 작업 환경 원칙 + SSH 키 경로 |
